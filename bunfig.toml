[install]
# Use Bun's built-in peer dependency resolution
peer = true

[build]
# Build configuration
target = "node"
format = "esm"
sourcemap = "external"

# Loaders for different file types
[build.loaders]
".md" = "text"
".txt" = "text"

[run]
# Run configuration
bun = true
